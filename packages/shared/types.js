// Pin shape — Zakari emits this from Socket.io
// { lat, lng, score, source, summary, timestamp }

// Session shape — Brandon returns this from panic endpoint
// { transcript, responseEn, responseEs, sessionId }
